export default function validate(stringToValidate: string): boolean;

export { ValidatorFactory, ValidatorType } from './validators/ValidatorFactory';
export { IValidator } from './validators/IValidator';
